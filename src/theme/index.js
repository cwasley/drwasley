import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
  palette: {
    background: {
      default: '#e0ffe9'
    },
    primary: {
      main: '#33006f'
    },
    secondary: {
      main: '#288222'
    },
    lightBackground: {
      main: '#f5ebff'
    }
  },
});

export default theme